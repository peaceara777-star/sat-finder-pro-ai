global:
  scrape_interval: 15s
  evaluation_interval: 15s

alerting:
  alertmanagers:
    - static_configs:
        - targets: []

rule_files: []

scrape_configs:
  - job_name: "sat-finder-pro-ai"
    static_configs:
      - targets: ["localhost:5000"]
    metrics_path: "/metrics"
    scheme: http
